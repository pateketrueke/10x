{
  "name": "@10x/lsp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js --stdio",
    "build": "esbuild server.js --bundle --platform=node --format=esm --outfile=dist/server.mjs"
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.11"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
